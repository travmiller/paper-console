fastapi
uvicorn[standard]
pydantic
requests

feedparser
icalendar
pytz
python-dateutil
astral
beautifulsoup4

# Hardware drivers (optional - only needed on Raspberry Pi)
pyserial
# Only install on Raspberry Pi / ARM Linux (avoid breaking WSL x86_64 installs)
RPi.GPIO; platform_system == "Linux" and (platform_machine == "aarch64" or platform_machine == "armv7l" or platform_machine == "armv6l")
qrcode[pil]
jsonschema
openai>=1.0.0
anthropic>=0.40.0
python-telegram-bot>=21.0
